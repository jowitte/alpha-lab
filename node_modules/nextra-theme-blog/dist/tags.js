import {
  getStaticTags
} from "./chunk-5IQBFQMY.js";
import "./chunk-PKUHTIDK.js";

// src/tags.tsx
import React from "react";
import { useSSG } from "nextra/ssg";
import Head from "next/head";
var TagTitle = () => {
  const { tag } = useSSG();
  return /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement(Head, null, /* @__PURE__ */ React.createElement("title", null, `Posts Tagged with ${tag}`)));
};
var TagName = () => {
  const { tag } = useSSG();
  return tag || null;
};
var getStaticPaths = () => {
  const tags = getStaticTags(globalThis.__nextra_internal__.pageMap);
  return {
    paths: tags.map((v) => ({ params: { tag: v } })),
    fallback: false
  };
};
var getStaticProps = ({ params: { tag } }) => {
  return {
    props: {
      ssg: {
        tag
      }
    }
  };
};
export {
  TagName,
  TagTitle,
  getStaticPaths,
  getStaticProps
};
